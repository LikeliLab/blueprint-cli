// Define the structure of the data your template expects
export interface ReadmeData {
  projectName: string;
  description: string;
  author: string;
  license: string;
  includeContributing: boolean;
  // Add any other variables needed by your template.ejs
}